# audio/sink.py
# Removed in Phase 4C. See legacy/chunk_pipeline/ for historical reference.
#
# In the streaming architecture, audio routes from StreamingSpeechProcessor directly to
# the streaming session via registered packet listeners — no sink layer is needed.
