Frontend (Next.js App Router + TypeScript + Tailwind)

Run locally:

```bash
cd frontend
npm install
npm run dev
```

The app uses `NEXT_PUBLIC_BACKEND_URL` to locate the backend (defaults to `http://localhost:8000`).
